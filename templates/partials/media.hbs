{{!--  Media  --}}
<div class="media {{~#if passthrough}} passthrough {{~/if}} {{~#if clickable}} clickable {{~/if}}">
    {{#if isVideo}}
        <video
            {{~#if metadata}} preload="metadata" {{~/if}}
            {{~#if eagerLoad}} data-src{{~else}} src{{~/if}}="{{src}}"
            {{~#if autoplay}} autoplay {{~/if}}>
        </video>
        {{#if videoIcon}}
            <i class="video-icon fa {{videoIcon}}"></i>
        {{/if}}
    {{else}}
        <img {{~#if lazyLoad}} loading="lazy" {{~/if}}{{~#if eagerLoad}} data-src{{~else}} src{{~/if}}="{{src}}" />
    {{/if}}
</div>
